# Frontend Environment Variables
VITE_APP_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3002

# API URL: empty = uses Vite proxy (/api → BACKEND_TARGET)
VITE_API_URL=

# Socket.IO: empty = connects to the same host as the page (via Vite proxy)
VITE_SOCKET_URL=

# Vite proxy target for /api requests
BACKEND_TARGET=http://localhost:3002

NODE_ENV=development
JWT_SECRET=bed5dbad14121bf320c6221e9287313b
PORT=3002
DB_USER=dirtysas
DB_PASSWORD=mypassword
DB_NAME=lms-project
# DB_USER=postgres
# DB_PASSWORD=admin
# DB_NAME=lms-project-2
DB_HOST=localhost
DB_PORT=5432
